use std::path::{Path, PathBuf};

fn main() {
    println!("test");

    let value = 0x1233445;
    let value_int = 123455;

    let p = Path::new(
        "D:/Workspace/binary-preview/binary-preview-lsp/target/debug/binary-preview-lsp.exe",
    );
}
